@prefix log: <http://www.w3.org/2000/10/swap/log#> .
@prefix : <urn:example:> .

# package surface with 2 triples
() log:package {
    :a :b :c.
    :d :e :f.
}.

# the package surface with the negation of those 2 triples does not blow an inference fuse
() log:package {
    () log:nand {
        :a :b :c.
        :d :e :f.
    }.
}.

# getting the content of package surfaces
(_:G) log:nand {
    () log:package _:G.
    () log:output {
        :result :is _:G.
    }.
}.
