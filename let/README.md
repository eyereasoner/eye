# Linking Every Thing

The goal is to link everything via CONSTRUCT WHERE.

Assert triples as
```
    CONSTRUCT {
        triples
    } WHERE {}
```

Derive conclusion as
```
    CONSTRUCT {
        conclusion
    } WHERE {
        premise
    }
```
